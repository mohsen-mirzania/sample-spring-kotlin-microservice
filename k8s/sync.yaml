apiVersion: batch/v1
kind: Job
metadata:
  name: before
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      restartPolicy: Never
      initContainers:
      - name: job-1
        image: ellerbrock/alpine-bash-git
        command: ["/bin/bash","-c","cd /data && git clone https://github.com/mohsen-mirzania/sample-spring-kotlin-microservice.git && cd sample-spring-kotlin-microservice && ls -l && ls -a | tee output.file"]
      containers:
      - name: sleep
        image: ellerbrock/alpine-bash-git
        command: ["/bin/bash","-c","cd /data && ls -a | tee output2.file"]
        volumeMounts:
        - name: vol
          mountPath: /data
      volumes:
      - name: vol
        persistentVolumeClaim:
          claimName: vol-claim
  backoffLimit: 0

